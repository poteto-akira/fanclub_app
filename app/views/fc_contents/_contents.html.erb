<!-- ファンクラブオーナーのタイムライン -->
<div class="orner-container">
  <!-- 現在のpostのコンテンツって意味 -->
  <% @post.contents.each do |content| %>
    <% if @post.user_id == content.user_id %>
    <li class="orner-timeline">
      <div class="content-member-img">
        <% if content.username == "芋あきら" %>
          <%= link_to image_tag("/user_images/akira.png"), "/home/profile/#{content.user_id}" %>
        <% elsif content.user_id %>
          <%= link_to image_tag("/user_images/default_image.jpg"), "/home/profile/#{content.user_id}" %>
        <% else %>
          <img src="<%= "/default_image.jpg" %>">
        <% end %>
      </div>
      <span class="created-at"><%= time_ago_in_words(content.created_at)%></span>
        <p class="orner-timeline-body">
          <!-- <%= link_to(content.content_body, "/fc_content/#{content.id}") %> -->
          <%= content.content_body %>
        </p>
      </li>
    <% end %>
  <% end %>
</div>

<!-- 会員のタイムライン -->
<div class="snap-drawers">
  <div class="snap-drawer snap-drawer-right">
    <!-- メニューの部分 -->
    <div class="member-timeline-container">
      <!-- 現在のpostのコンテンツって意味 -->
      <% @post.contents.each do |content| %>
      <!-- ファンクラブオーナー以外のユーザー -->
        <% if @post.user_id != content.user_id %>
        <li class="member-timeline">
          <div class="content-member-img">
            <% if content.username == "芋あきら" %>
              <%= link_to image_tag("/user_images/akira.png"), "/home/profile/#{content.user_id}" %>
            <% elsif content.user_id %>
              <%= link_to image_tag("/user_images/default_image.jpg"), "/home/profile/#{content.user_id}" %>
            <% else %>
              <img src="<%= "/default_image.jpg" %>">
            <% end %>
          </div>
          <span class="created-at"><%= content.username %></span>
          <span class="created-at"><%= time_ago_in_words(content.created_at)%></span>
          <div class="member-timeline-body">
            <%= content.content_body %>
          </div>
        </li>
        <% end %>
      <% end %>
    </div>
  </div>

  <div class="new-content">
    <!-- @post.idは現在のfc.idのこと -->
    <%= form_tag("/fc_content/create/#{@post.id}", :remote => true) do  %>
      <input name="content_body" placeholder="ここでコメントを送ってみよう！" type="text" class="content-new" id="input1" autocomplete="off"></input>
      <input class="submit" id="submit1" type="submit" value="送る">
    <% end %>
    <!-- フォームに１文字以上入力しないとsubmitボタンが有効にならない -->
    <script>
      $(function(){
        if ($("#input1").val().length == 0) {
          $("#submit1").prop("disabled", true);
        }
        $("#input1").on("keydown keyup keypress change", function() {
          if ($(this).val().length < 1) {
            $("#submit1").prop("disabled", true);
          } else {
            $("#submit1").prop("disabled", false);
          }
        });
      });
    </script>
  </div>

  <!-- <tr class="contents" data-id=<%= @content.id %>>
  <script>
  $(function(){
    $(function(){
      setInterval(update, 10000);
      //10000ミリ秒ごとにupdateという関数を実行する
    });

    function update(){
      var content_id = $('.contents:last').data('id');//一番最後にある'contents'というクラスの'id'というデータ属性を取得し、'content_id'という変数に代入
      $.ajax({ //ajax通信で以下のことを行う
          url: location.href, //urlは現在のページを指定
          type: 'GET', //メソッドを指定
          data: { //railsに引き渡すデータは
            content: { id: content_id } //このような形(paramsの形をしています)で、'id'には'content_id'を入れる
          },
          dataType: 'json' //データはjson形式
      })
      .always(function(data){ //通信したら、成功しようがしまいが受け取ったデータ（@new_message)を引数にとって以下のことを行う
        $.each(data, function(i, data){ //'data'を'data'に代入してeachで回す
          buildMESSAGE(data); //buildMESSAGEを呼び出す
        });
      });
    }
  });
  </script> -->
