<%= form_tag("/fc_content/create/#{@post.id}") do %>
  <% if @post.errors.any? %>
    <% @post.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>
  <input name="content_body" placeholder="ここでコメントを送ってみよう！" type="text" class="content-new" autocomplete="off"></input>
  <input class="submit" type="submit" value="投稿">
<% end %>
